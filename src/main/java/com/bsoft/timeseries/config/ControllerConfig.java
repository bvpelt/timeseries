package com.bsoft.timeseries.config;

import com.bsoft.timeseries.api.*;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

//@Configuration
public class ControllerConfig {
/*
//    @Bean
    // Gebruik de exacte naam van de bean die de generator zou maken
    public AddressesApiController addressesApiController(AddressesApiDelegate mainAddressDelegate) {
        return new AddressesApiController(mainAddressDelegate);
    }

//    @Bean
    // Gebruik de exacte naam van de bean die de generator zou maken
    public AgreementsApiController agreementsApiController(AgreementsApiDelegate mainAgreementsApiDelegate) {
        return new AgreementsApiController(mainAgreementsApiDelegate);
    }

//    @Bean
    // Gebruik de exacte naam van de bean die de generator zou maken
    public PersonRelationsApiController personRelationsApiController(PersonRelationsApiDelegate mainPersonRelationsApiDelegate) {
        return new PersonRelationsApiController(mainPersonRelationsApiDelegate);
    }

//    @Bean
    // Gebruik de exacte naam van de bean die de generator zou maken
    public PersonsApiController personsApiController(PersonsApiDelegate mainPersonsApiDelegate) {
        return new PersonsApiController(mainPersonsApiDelegate);
    }

 */
}